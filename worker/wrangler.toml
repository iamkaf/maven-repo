name = "maven-metadata-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"
workers_dev = false

# R2 bucket binding for artifact storage
[[r2_buckets]]
binding = "R2"
bucket_name = "maven-kaf-sh-artifacts"

# Routes are managed in Terraform via cloudflare_workers_route
# Use wrangler dev --local for local development
