{
  "name": "maven-metadata-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Maven metadata API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.57.1"
  },
  "dependencies": {
    "fast-xml-parser": "^4.3.2"
  }
}
